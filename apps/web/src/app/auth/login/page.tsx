'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { useAuth } from '@/lib/auth';
export default function LoginPage(){ const { login }=useAuth(); const router=useRouter(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{await login(email,password);router.push('/');}catch(err){setError(err instanceof Error?err.message:'Не получилось войти');}finally{setBusy(false);}} return <main className="form-page"><form className="form-card" onSubmit={submit}><div className="brand" style={{padding:0,marginBottom:28}}><span className="brand-mark">✦</span><span>Talqyla</span></div><h1>С возвращением</h1><p>Продолжи тренировку там, где остановился.</p>{error&&<div className="error">{error}</div>}<div className="field"><label htmlFor="email">Email</label><input id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" /></div><div className="field"><label htmlFor="password">Пароль</label><input id="password" type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Минимум 8 символов" /></div><button className="button primary" style={{width:'100%'}} disabled={busy}>{busy?'Входим...':'Войти'}</button><div className="form-footer">Нет аккаунта? <Link href="/auth/register">Создать его</Link></div></form></main>;}
