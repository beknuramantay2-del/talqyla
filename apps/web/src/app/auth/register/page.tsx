'use client';
import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { useAuth } from '@/lib/auth';
export default function RegisterPage(){ const { register }=useAuth(); const router=useRouter(); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [parentEmail,setParentEmail]=useState(''); const [password,setPassword]=useState(''); const [consent,setConsent]=useState(false); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); async function submit(e:FormEvent){e.preventDefault();setError(''); if(!consent){setError('Нужно согласие родителя или законного представителя.');return;}setBusy(true);try{await register(email,password,name,parentEmail,consent);router.push('/');}catch(err){setError(err instanceof Error?err.message:'Не получилось создать аккаунт');}finally{setBusy(false);}} return <main className="form-page"><form className="form-card" onSubmit={submit}><div className="brand" style={{padding:0,marginBottom:28}}><span className="brand-mark">✦</span><span>Talqyla</span></div><h1>Начни с одного раунда</h1><p>Соберём твой профиль и подберём первый уровень сложности.</p>{error&&<div className="error">{error}</div>}<div className="field"><label htmlFor="name">Как тебя зовут?</label><input id="name" required value={name} onChange={e=>setName(e.target.value)} placeholder="Например, Арчи" /></div><div className="field"><label htmlFor="email">Твой email</label><input id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" /></div><div className="field"><label htmlFor="parentEmail">Email родителя</label><input id="parentEmail" type="email" required value={parentEmail} onChange={e=>setParentEmail(e.target.value)} placeholder="parent@example.com" /></div><div className="field"><label htmlFor="password">Придумай пароль</label><input id="password" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Буквы и цифры, от 8 символов" /></div><label className="consent"><input type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)} /> <span>Мой родитель или законный представитель согласен на обработку учебных ответов и голосовых транскрипций для работы тренера.</span></label><button className="button primary" style={{width:'100%'}} disabled={busy}>{busy?'Создаём профиль...':'Создать профиль'}</button><div className="form-footer">Уже есть аккаунт? <Link href="/auth/login">Войти</Link></div></form></main>;}
